# animalica dark vision compatibility patch
a simple patch to add night sight via the dark vision mod for some of the models from animalica
support rollout will be slow, since I'm adding these as I need them right now. if someone knows how to expose json values to a config file with only a content mod, lemme know
